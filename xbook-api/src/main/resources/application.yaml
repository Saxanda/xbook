server:
  port: 8080

spring:
  jpa:
    generate-ddl: true
    show-sql: true

  mail:
    host: smtp_host # Change host name to actual one exp. <<smtp.gmail.com>>
    port: 587 # Change SMTP-server port to actual one
    username: username # Change username to actual one
    password: password # Change password to actual one

  h2:
    console:
      enabled: true
      path: /h2

  datasource:
    url: jdbc:h2:/src/main/resources/xbook.db
    username: sax
    password: 123

server:
  port: 8080

spring:
  servlet:
    multipart:
      max-file-size: 10MB

  profiles:
    active: local

  springdoc:
    swagger-ui:
      enabled: true
      path: /swagger-ui.html
    api-docs.path: /api-docs